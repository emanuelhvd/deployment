# ============================================
# TILEDESK - TEMPLATE DE CONFIGURAÇÃO
# ============================================
# Copie este arquivo para criar seus ambientes:
#   cp .env.example .env.dev
#   cp .env.example .env.staging
#   cp .env.example .env.production

# === BASE URLs ===
# Desenvolvimento: http://localhost:8081 e ws://localhost:8081
# Staging/Produção: https://seu-dominio.com e wss://seu-dominio.com
EXTERNAL_BASE_URL=http://localhost:8081
EXTERNAL_MQTT_BASE_URL=ws://localhost:8081

# === RabbitMQ (Message Queue) ===
# IMPORTANTE: Mude as senhas padrão em staging/produção!
RABBITMQ_ERLANG_COOKIE=change_me_in_production
RABBITMQ_DEFAULT_USER=admin
RABBITMQ_DEFAULT_PASS=change_me_in_production

# === OpenAI / GPT Integration ===
# Obtenha sua chave em: https://platform.openai.com/api-keys
# Deixe CHANGEIT se não for usar IA
GPTKEY=CHANGEIT

# === Email Configuration ===
# Configure para enviar emails de notificação/recuperação de senha
EMAIL_ENABLED=false
EMAIL_HOST=smtp.gmail.com
EMAIL_USERNAME=seu-email@gmail.com
EMAIL_PASSWORD=sua-senha-ou-app-password
EMAIL_SECURE=true
EMAIL_PORT=465
EMAIL_FROM_ADDRESS=noreply@suaempresa.com

# === Firebase (Push Notifications - Opcional) ===
# Configure se quiser notificações push no mobile
FIREBASE_APIKEY=
FIREBASE_AUTHDOMAIN=
FIREBASE_DATABASEURL=
FIREBASE_PROJECT_ID=
FIREBASE_STORAGEBUCKET=
FIREBASE_MESSAGINGSENDERID=
FIREBASE_APP_ID=
FIREBASE_VAPID=
FIREBASE_PRIVATE_KEY=
FIREBASE_CLIENT_EMAIL=

# === Facebook Messenger Integration (Opcional) ===
# Para integrar chat com Facebook Messenger
FB_APP_ID=
FB_APP_SECRET=

# === License Key (Opcional) ===
# Deixe vazio para usar Community Edition (gratuita)
LICENSE_KEY=

# === BRANDING / IDENTIDADE VISUAL ===
# Customize a aparência do TileDesk com sua marca

# Logo da empresa (URL público acessível ou caminho relativo)
# Formatos: PNG, SVG (recomendado: 200x50px)
# Exemplo: https://suaempresa.com/assets/logo.png
BRAND_SRC=

# JavaScript customizado (avançado)
# Use para injetar CSS/JS customizado e mudar cores, fontes, etc
# Exemplo: https://suaempresa.com/tiledesk-custom.js
REMOTE_JS_SRC=

# Banner promocional no dashboard (opcional)
# JSON com configuração de banner: {text, url, color}
PROMO_BANNER_URL=

# ============================================
# DICAS DE USO
# ============================================
#
# RODAR CADA AMBIENTE:
#   Development:  docker-compose --env-file .env.dev up
#   Staging:      docker-compose --env-file .env.staging up
#   Production:   docker-compose --env-file .env.production up -d
#
# ACESSAR:
#   URL: http://localhost:8081/
#   Email: admin@tiledesk.com
#   Senha: superadmin
#
# LOGS:
#   docker-compose logs -f
#
# PARAR:
#   docker-compose down
#
# LIMPAR TUDO (cuidado - apaga dados!):
#   docker-compose down -v
